package team072.action;

import battlecode.common.GameActionException;
import battlecode.common.RobotController;

/**
 * State for archon, build to get score
 */

public class Building {

	public static void act(RobotController myRC, Object... objects)
			throws GameActionException {
		// TODO Auto-generated method stub

	}

	/*
	 * build a new building of buildType and transfer all remaining flux to that
	 * building
	 */
	// @Override
	// public void action(RobotType buildType) throws GameActionException{
	// if (myRC.getFlux() < buildType.spawnFluxCost()){
	// System.out.println("Insufficient flux to build");
	// myRC.yield();
	// }
	// alliedBuildings = senseAlliedBuildings();
	// //TODO: decide where to build the next building
	// if (alliedBuildings.size() > 0) {
	// MapLocation locToBuild = alliedBuildings.get(0);
	// RobotInfo info = build(locToBuild, buildType);
	// myRC.transferFlux(locToBuild, info);
	// myRC.yield();
	// }
	// myRC.yield();
	// }
	//	
	// /*
	// * return a list of locations for all our buildings within sensing range
	// */
	// public ArrayList<MapLocation> senseAlliedBuildings () {
	// Robot[] nearbyRobot = myRC.senseNearbyGoundRobots();
	// ArrayList<MapLocation> output = new ArrayList<MapLocation>();
	// for (Robot r: nearbyRobot) {
	// RobotInfo info = myRC.senseRobotInfo();
	// if ((info.team == myTeam) && (info.type.isBuilding())) {
	// output.add(info.location);
	// }
	// }
	// return output;
	// }
	//	
	// /*
	// * spawn a bulding of type buildType at loc
	// */
	// public RobotInfo build(MapLocation loc, RobotType buildType) {
	// MapLocation currentLoc = myRC.getLocation();
	// tangentBug(currentLoc, new MapLocation(loc.getX() - 1, loc.getY()));
	// myRC.setDirection(Direction.EAST);
	// if (myRC.senseGroundRobotAtLocation() == null)
	// myRC.spawn(buildType);
	// myRC.yield();
	// Robot newBuilding = myRC.senseGoundRobotAtLocation(loc);
	// return myRC.senseRobotInfo(newBuilding);
	// }

}
