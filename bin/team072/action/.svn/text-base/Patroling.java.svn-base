package team072.action;

import battlecode.common.GameActionException;
import battlecode.common.RobotController;

public class Patroling {
//	private RobotController myRC;
//	private MapLocation lastDes = null;
//	private MapLocation currentDes = null;
//	private Map myMap;

//	public Patroling(RobotController rc) {
//		myRC = rc;
//		myMap = new Map(myRC);
//	}

	public static void act(RobotController myRC, Object... objects)
			throws GameActionException {
//		Direction dir = myMap.tangentBug(myRC.getLocation(),
//				(MapLocation) objects[0]);
//		if (!myRC.getDirection().equals(dir)) {
//			// myRC.yield();
//			myRC.setDirection(dir);
//		}
		myRC.yield();
		myRC.moveForward();
	}

}
